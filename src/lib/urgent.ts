// Shape for a "Hitno" (urgent-attention) item shown on the dashboard landing
// page. Placeholder data until the per-entity editors are wired; each item links
// to the section it relates to. Used by the page and the reusable <UrgentItem>.
export type Urgent = { id: string; title: string; body: string; href: string };
